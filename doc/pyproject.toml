[project]
name = "cimc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-docx>=1.2.0",
    "sympy>=1.14.0",
]

[tool.uv.workspace]
members = [
    "test",
]
